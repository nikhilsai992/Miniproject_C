#include<inc.h>
/**
 * @brief To display the total number of books in the library
 * 
 * @param l - struct object
 * @param count - total number of books
 */
void book_count(struct library *l, int count)
{
    printf("\n No of books in library : %d\n", count);
    
}